<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- Log message format -->
        <layout>
            <pattern>
                %green(%d{dd-MM-yyyy HH:mm:ss.SSS}) [%thread] %highlight(%5level) %yellow(%20.20logger{15}) %-2.2L - %msg%n
            </pattern>
        </layout>
    </appender>

    <!-- Setting the root level of logging to INFO -->
    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>